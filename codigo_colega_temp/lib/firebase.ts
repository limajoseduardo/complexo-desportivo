// Firebase integration disabled.
// Replace these with new service initialization when ready.

export const requestFirebaseToken = async () => null;

export const setupForegroundMessaging = () => {
  console.warn("Firebase messaging disabled");
};

export const messaging = null;

export const app = null;
